name: Run Repository_Dispatch

on:
  repository_dispatch:
    types: [run_test]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - run: 'echo "field: ${{ github.event.client_payload.admin_version }}"'
      - run: 'echo "client_payload: ${{ toJson(github.event.client_payload) }}"'

      - name: Cache Docker dependency
        uses: actions/cache@v2
        with:
          path: ~/var/lib/docker/overlay2
          key: ${{ runner.os }}-docker-images

      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - run: npm install
      - run: $(npm bin)/cypress run --spec ./cypress/integration/single-agent/single-agent.spec.js --env startApplicationTaskName=startPetclinic,initialApplicationBuildVersion=0.1.0,secondApplicationBuildVersion=0.5.0,startApplicationTestsTaskName=startPetclinicAutoTests  || true
      - name: Archive cypress videos results
        uses: actions/upload-artifact@v2
        with:
          name: cypress-videos
          path: cypress/videos


# Generate Personal Github token with workflow scopes.
# Run action
# curl -H "Authorization: token xxx" \
#    -H 'Accept: application/vnd.github.everest-preview+json' \
#    "https://api.github.com/repos/Drill4J/e2e/dispatches" \
#    -d '{ "event_type": "run_test", "client_payload": {"admin_version": "1.0.58"}}'
